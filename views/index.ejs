<html>
<title>College Event Manager</title>
<head>
  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">

  <!-- jQuery library -->
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js"></script>

  <!-- Latest compiled JavaScript -->
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

  <!-- AngularJS and its route -->
  <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.5.7/angular.js"></script>
  <script src="http://maps.googleapis.com/maps/api/js?key=AIzaSyCYEiUAFh3-Ca91pCPgn8a-DFX7OEL8GZQ"></script>

  <!-- Using the show and hide eye for password -->
  <script src="https://kit.fontawesome.com/962d9b3821.js" crossorigin="anonymous"></script>

  <!-- Function to show and hide password -->
  <script>
    $(document).ready(function()
    {
      $(".hide-password").click(function()
      {
        var passwordField = $("#password");
        var icon = $(".hide-password");
    
        if (passwordField.attr("type") === "password")
        {
          passwordField.attr("type", "text");
          icon.removeClass("fa-eye-slash").addClass("fa-eye");
        }
        else
        {
          passwordField.attr("type", "password");
          icon.removeClass("fa-eye").addClass("fa-eye-slash");
        }
      });
    });
    
  </script>

  <style>
    body {
      background-image: url("../images/SigninBackground.jpg");
      background-size: cover;
    }
    /* Setting dimensions and styling the signin background */
    #signinBackground {
      align-items: center;
      background-color: #C5C7C8;
      border: 3px solid #FEF3B3;
      border-radius: 10px;
      display: flex;
      flex-direction: column;
      position: absolute;
      text-align: center;
      transform: translate(-50%, -50%);

      height: 472px;
      width: 354px;

      left: 50%;
      top: 50%;
    }

    /* Setting and styling the background of the title bar */
    #collegeEventManager {
      align-items: center;
      background-color: #FEF3B3;
      border-radius: 6px;
      color: #000000;
      display: flex;
      font-size: 14pt;
      font-weight: bold;
      justify-content: center;
      margin-top: 1.5em;
      margin-bottom: 0.5em;
      transform: translateX(-50%, -50%);

      height: 50px;
      width: 299px;
    }
    
    /* Setting and styling the username and password field */
    #usernameField text, #passwordField text {
      color: #000000;
      display: inline-block;
      margin-left: 1em;
      margin-right: 0em;
      width: 85px;
    }
    #username, #password {
      color: #656565;
      background-color: #FBFBFB;
      border: none;
      border-radius: 4px;
      margin-bottom: 0.5em;
      margin-right: 1em;
      text-align: left;
      text-indent: 6px;

      height: 30px;
      width: 210px;
    }
    /* Removing the "focus ring" when the username and password field is clicked */
    #username:focus, #password:focus {
      outline: none;
    }
    /* Setting and styling inside the username and password field */
    #username::placeholder, #password::placeholder {
      color: #656565;
      background-color: #FBFBFB;
      border: none;
      border-radius: 4px;
      margin-bottom: 0.5em;
      margin-right: 1em;
      text-align: left;
    }
    /* Setting and styling the show and hide eye password icon */
    #eyeIcon {
      color: #656565;
      position: absolute;
      top: 139px;
      right: 35px;
      transform: translateY(-50%);
      cursor: pointer;
    }

    /* Setting and styling the "NO ACCOUNT?" text */
    #noAccount {
      color: #000000;
      font-size: 10pt;
      margin-left: 1.1em;
      text-align: left;
    }
    
    /* Setting and styling the dancing photo */
    #dancingPhoto img {
      text-align: center;
      margin-bottom: 0.5em;

      height: 180px;
    }

    /* Styling the error message (if there is one) */
    #errorMessage {
      color: rgb(255, 0, 0);
      font-size: 10pt;
      white-space: nowrap;
    }

    /* Setting and styling the "SIGN IN" button */
    #signinButton {
      background-color: #FEF3B3;
      border: none;
      border-radius: 8px;
      color: #000000;
      cursor: pointer;
      font-size: 18pt;
      margin-top: 0.5em;
      text-align: center;

      height: 37px;
      width: 299px;

      position: absolute;
      top: 390px;
      left: 50%;
      transform: translateX(-50%);
    }
    /* Changing the "SIGN IN" button color when hovered over */
    #signinButton:hover {
      background-color: #F1E6AD ;
    }
  </style>
</head>

<body>
  <center>
    <div id="signinBackground">
      <text id="collegeEventManager">COLLEGE EVENT MANAGER</text>
      <form id="signinForm" class="form" action="/" method="post">
        <div id="usernameField">
          <text>USERNAME:</text>
          <input id="username" name="username" type="text" autocomplete="off" placeholder="USERNAME">
        </div>
        <div id="passwordField">
          <text>PASSWORD:</text>
          <input id="password" name="password" type="password" autocomplete="off" placeholder="PASSWORD">
          <i id="eyeIcon" class="fas fa-eye-slash hide-password"></i>
       </div>
        <div id="noAccount">
          <p>Don't have an account? <a href="/register"><u>Sign up</u></a></p>
        </div>
        <div id="dancingPhoto">
          <img src="images/DancingPhoto.png" alt="SigninPhoto">
        </div>
        <div id="errorMessage">
          <text><% if (message.length > 0) {%><%=message%><%}%></text>        
        </div>
        <button id="signinButton" type="submit">SIGN IN</button>
      </form>
    </div>
  </center>
</body>

</html>